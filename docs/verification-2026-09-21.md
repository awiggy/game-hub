# 2026-09-21 本地验收记录

## 本次改动

- 游戏注册表从 10 款更新为 50 款，包含之前未登记的 38 款，以及新增的石头剪刀布、骰子对决。
- 未完成的星环躲避元数据移至 `drafts/orbit/` 保留，避免缺失入口阻塞正式构建。
- 修复 12 款现有游戏在初始化之前启动绘图循环的问题：`asteroids`、`basketball`、`catcheggs`、`fruitcut`、`knifehit`、`ninjadash`、`shielddef`、`skiing`、`spaceinvaders`、`stacker`、`tabletennis`、`towerdef`。统一在状态初始化完成后首次调用 `loop()`。
- `npm run build` 先运行静态检查，成功后生成注册表并构建。现有 GitHub Actions 使用同一构建命令。

## 已执行的验证

| 验证 | 结果 |
|---|---|
| `npm run check`（文件、脚本语法、元数据、状态钩子） | 50 款通过 |
| `npm run build` | 通过，输出 `dist/` |
| Chrome / Playwright 逐个打开 50 个游戏页面 | 全部 HTTP 200，有 `__hub` 状态钩子，没有未捕获 JavaScript 异常 |
| 逐个点击可见的开始按钮，观察初始游戏状态 | 无未捕获异常，分数均为有限数值；五子棋、记忆翻牌和扫雷的 started 仍需首次落子/翻牌/开格才会改变 |
| 首页与游戏库 | 首页显示新增游戏及 50 款计数；游戏库有 50 张卡片 |
| 两款新增游戏：确定性随机输入下连续得分至结束 | 分数、结束状态、结果遮罩正确 |
| 两款新增游戏：结算期间重复输入、结算期间重开 | 不重复计分；已取消的旧回合不会污染新局 |
| 两款新增游戏：平局、刷新保留纪录、重开 | 通过 |
| 石头剪刀布：电脑胜利 | 正确结束并显示电脑胜利 |
| 两款新增游戏：浏览器存储不可用 | 仍能开始及游玩 |
| 两款新增游戏：320px 手机视口与触屏点按 | 可以完成回合，没有横向溢出，截图检查通过 |
| `git diff --check` | 通过 |

浏览器检查通过临时 Playwright 脚本在本地 Chrome 执行，未给项目添加新的测试依赖。以上属于加载、初始交互和新游戏重点流程验收；不代表已经完整通关其余 48 款，也不覆盖它们的全部关卡、平衡性或长时间运行。

本记录对应本地验收阶段；用户随后要求推送到 GitHub，线上发布状态以该提交对应的 GitHub Actions 结果为准。
