# 游戏大全 GAME HUB

一个点开即玩的浏览器小游戏平台：收录 **50 款小游戏，覆盖 10 个分类**，支持分类浏览、游戏详情和嵌入式试玩。基于 Vue 3 + Vite，采用蓝黄配色的像素街机风格。

每款游戏使用自包含 HTML，新增游戏可自动校验并登记；构建前执行静态检查。平台还提供 AI 创作演示页与模型配置界面，实际生成后端尚未接入。

- **线上地址**：https://awiggy.github.io/game-hub/
- **仓库**：https://github.com/awiggy/game-hub
- **部署**：push 到 main 即自动构建发布（GitHub Actions → GitHub Pages）

## 开发

```bash
npm install
npm run dev      # http://localhost:5173（启动前自动重建注册表）
npm run check    # 检查每款游戏的文件、脚本语法、元数据与状态钩子
npm run build    # 先检查并重建注册表，再输出 dist/；检查失败会阻止发布
```

## 当前进度（2026-09-21）

- **50 款小游戏已登记**，覆盖 10 个分类；首页和游戏库均使用最新注册表。
- 本轮新增：石头剪刀布、骰子对决，参考 [wanghao221/moyu](https://github.com/wanghao221/moyu) 中实际提供的源码。
- 未完成的星环躲避资料保留在 `drafts/orbit/`，不计入 50 款，不参与发布。
- 参考来源、版本与适配说明见 [SOURCES.md](SOURCES.md)；项目方向和待办见 [PROJECT_MEMORY.md](PROJECT_MEMORY.md)。
- AI 创作后端仍未实现，当前更新聚焦小游戏库与构建检查。

## 游戏货架（50 款）

| 游戏 | 分类 | 操作 |
|---|---|---|
| 🎲 骰子对决 | 休闲 / 棋牌 | 点击掷骰或按空格，双方同时掷骰。先得 5 分获胜；R 重新开局。 |
| ✊ 石头剪刀布 | 休闲 / 街机 | 点击出拳，或按 1 石头、2 布、3 剪刀。先得 10 分获胜；空格开始，R 重新开局。 |
| 🐜 蚂蚁帝国 | 模拟 / 策略 | 点击草地放蚂蚁；食物够了点下方升级按钮；倒计时结束结算。 |
| 🏹 百步穿杨 | 体育 / 解谜 | ← → 调角度，按住空格/屏幕蓄力，松开放箭；留意顶部风向。 |
| 🪨 星际小行星 | 射击 / 动作 | ← → 旋转，↑ 推进，空格射击；屏幕会环绕。 |
| 🏀 街头投篮 | 体育 / 街机 | 按住空格或屏幕蓄力，松开投篮；力度决定飞行远近。 |
| 🔀 大小对决 | 棋牌 / 休闲 | 点「更大」或「更小」按钮押注；对了继续，错了结算。 |
| 🃏 21点 | 棋牌 / 休闲 | 点「要牌」补一张或「停牌」开牌；A 可当 1 或 11。 |
| 🥚 接鸡蛋 | 街机 / 休闲 | ← → 或 A D 移动篮子；手机拖动；接蛋得分、躲炸弹。 |
| 🐔 横穿车流 | 动作 / 竞速 | ↑↓←→ 或滑动屏幕逐格移动小鸡；前进 1 行 +1 分，被车撞到即结束。 |
| 🛗 电梯调度员 | 模拟 / 解谜 | 点击楼层按钮让电梯移动；停靠自动接送乘客；别让乘客等太久。 |
| 🌾 摸鱼农场 | 模拟 / 休闲 | 点击空地种菜（花 1 金币），成熟后点击收获；金币够了就点升级按钮。 |
| 🔗 流水连线 | 解谜 / 休闲 | 从一个端点按住拖到同色另一个端点；全部颜色连通过关。 |
| 🍉 切水果 | 动作 / 街机 | 按住鼠标或手指滑动切水果；切开 +1，连击有加成；切到炸弹或漏 3 个水果即结束。 |
| 🔍 猜数字 1A2B | 解谜 / 休闲 | 点数字键组装 4 位猜测，点确认；根据 xAyB 反馈推理出答案。 |
| 🗼 汉诺塔 | 解谜 / 策略 | 点击柱子拿起顶盘，再点另一柱放下；大盘不能压小盘；全部移到第三柱即过关。 |
| 🔪 飞刀挑战 | 动作 / 休闲 | 空格或点击发刀；每关插满 6+关卡数 把过关；与已插的刀相撞即结束。 |
| 💡 点灯谜题 | 解谜 / 策略 | 点击格子翻转它与上下左右 5 格；全部熄灭过关；方向键+空格也可操作。 |
| 🍬 甜蜜三消 | 解谜 / 休闲 | 点击选中糖果，再点击相邻糖果交换；三连及以上自动消除。 |
| 🌀 迷宫探险 | 解谜 / 街机 | 方向键 / WASD / 滑动屏幕移动；从左上 ⭐ 走到右下 🚪。 |
| 🔢 六宫数独 | 解谜 / 策略 | 点击空格选中，再点底部数字填入（再点同格清除）；填满且无冲突即过关。 |
| 🥷 忍者酷跑 | 动作 / 街机 | 空格或点击屏幕跳跃，空中可再按一次二段跳；撞到尖刺、方块或掉进断坑即结束。 |
| ⚽ 点球大战 | 体育 / 动作 | 点击九宫格任一格射门；守门员会扑向某格；共 5 次机会。 |
| 🐱 云养喵 | 模拟 / 休闲 | 点击三个动作按钮（注意冷却）；饱食度或心情归零则结束。 |
| 🎹 琴键快按 | 街机 / 休闲 | 点击黑块所在列，或按 D F J K；黑块触底即失败。 |
| ⚡ 极速反应 | 街机 / 休闲 | 红色时等待，变绿的瞬间点击整块区域；共 20 回合。 |
| ⚫ 黑白棋 | 棋牌 / 策略 | 点击高亮的合法格落子；夹住的白子会被翻成黑子。 |
| 🛡️ 护盾守卫 | 动作 / 射击 | ← → 调整护盾转速，或按住拖动跟随角度；挡弹 +1，子弹击中核心 -1 命，共 3 命。 |
| 🏪 街角小店 | 模拟 / 策略 | 点 ＋ 进货，点「开张营业」结算次日售价并卖出全部库存，共 10 天。 |
| 🎵 记忆色序 | 解谜 / 休闲 | 看完闪烁序列后，按相同顺序点击色块。 |
| 🎿 高山滑雪 | 体育 / 竞速 | ← → 或 A D 左右移动；手机端触摸屏幕左右半边。 |
| 🔢 数字华容道 | 解谜 / 休闲 | 点击空格旁的数字块滑动，或用方向键；将 1~15 按顺序复原即过关。 |
| 📦 像素推箱子 | 解谜 / 策略 | 方向键 / WASD / 滑动移动；把箱子推到 ✦ 目标点上。 |
| 👾 太空侵略者 | 射击 / 街机 | ← → 移动，空格射击；别让敌人降到底线。 |
| 📦 叠叠乐 | 街机 / 解谜 | 点击 / 空格放下移动中的横条；重叠越多塔越稳。 |
| 🏓 乒乓快打 | 体育 / 街机 | 移动鼠标 / ↑↓ 键 / 上下滑动控制球拍上下；接住球并打回对方半场。 |
| ⭕ 井字棋 | 棋牌 / 策略 | 点击空格落子，三子连线即胜；连胜越多分越高。 |
| 🗼 像素塔防 | 策略 / 动作 | 点击空地建炮塔（50 金币）；点按钮召唤下一波。 |
| 🧮 速算24点 | 棋牌 / 解谜 | 点两张牌 + 运算符合成；目标是合成 24；卡住可换题（扣分）。 |
| 🔨 打地鼠 | 街机 / 休闲 | 点击 / 触碰冒头的地鼠 🐹 得 1 分，金鼠 ⭐ +3 分；键盘 1-9 对应九洞，空格开始，限时 30 秒。 |
| 🔢 数字合合乐 2048 | 解谜 / 休闲 | 方向键 / WASD 滑动；手机端上下左右滑动屏幕。 |
| 🧱 疯狂打砖块 | 街机 / 动作 | 移动鼠标或左右方向键控制挡板；空格 / 点击发射弹球；手机端拖动屏幕。 |
| 🐤 像素小鸟 | 街机 / 休闲 | 空格 / 鼠标点击 / 触屏点按让小鸟向上飞，保持节奏穿过缝隙。 |
| ⚫ 五子棋 | 棋牌 / 策略 | 点击棋盘交叉点落子；人机模式你执黑，AI 执白；可随时切换模式或重开。 |
| 🧠 记忆翻牌 | 休闲 / 解谜 | 点击卡片翻开，一次翻两张；配对成功保持明面，失败自动盖回。 |
| 💣 经典扫雷 | 解谜 / 策略 | 左键 / 点击翻开格子；右键或开启🚩模式后点击插旗；翻开所有安全格即胜利。 |
| ✈️ 雷霆打飞机 | 射击 / 动作 | 鼠标移动或拖动屏幕控制战机位置，← → 也可以移动；火力全自动，专心走位。 |
| 🏎️ 极速避让 | 竞速 / 休闲 | ← → 或 A D 变道；手机端点击屏幕左 / 右半边变道。 |
| 🐍 像素贪吃蛇 | 街机 / 休闲 | 方向键 / WASD 转向；空格暂停；手机端滑动屏幕转向。 |
| 🧊 俄罗斯方块 | 解谜 / 街机 | ← → 移动，↑ 旋转，↓ 软降，空格硬降；手机端使用画面下方按钮。 |

## 结构

```
├── index.html / vite.config.js / package.json   # 门户应用（Vite + Vue3）
├── scripts/build-registry.js                    # ★ 注册脚本：扫描 games/ 生成注册表
├── scripts/check-games.js                       # 静态检查（正式构建前自动执行）
├── src/
│   ├── pages/          # 首页 / 游戏库 / 游戏详情
│   ├── components/     # 导航、游戏卡片、试玩框架
│   ├── data/games.json # 注册表（脚本生成，勿手改）
│   └── game-meta.js    # 分类元数据（名称/图标/配色）
├── drafts/             # 未完成游戏资料，不注册、不发布
└── games/              # ★ 每个游戏 = 一个自包含目录（Vite publicDir）
    ├── snake/
    │   ├── meta.json   #   游戏元数据（注册来源）
    │   └── index.html  #   自包含游戏页，详情页 iframe 嵌入试玩
    └── .../            #   tetris / breakout / flappy / planewar / minesweeper ...
```

## 如何新增一个游戏

1. 在 `games/` 下新建与游戏 id 同名的文件夹
2. 放入自包含的 `index.html`（纯静态、无构建、无外部依赖）和 `meta.json`
3. 运行 `npm run check`，检查文件、JavaScript 语法和元数据
4. 运行 `npm run registry`（dev / build 前会自动执行），生成门户使用的清单
5. 在浏览器验证开始、操作、结束、重开及触屏操作，再运行 `npm run build`

静态检查不能保证游戏逻辑正确；浏览器验收仍是发布前的必要步骤。缺少入口或存在脚本语法错误时，构建会中止，GitHub Actions 不会发布这次产物。

`meta.json` 必填字段：`id`（=目录名）、`name`、`emoji`、`categories`（见 `src/game-meta.js` 的 10 个分类）、`summary`、`description`、`howToPlay`、`entry`。

## 游戏开发约定

- 单文件 HTML，零外部依赖，深色主题（`#0f1118` 背景 + 蓝黄强调色）
- 暴露 `window.__hub = { score, over, started }` 状态钩子——供中台自动验收与后续「AI 生成游戏」的沙箱检测使用
- 支持键盘与触屏，最高分 / 纪录用 localStorage 持久化（key 加游戏前缀）

## 路线图

- [x] 门户骨架（首页 / 游戏库 / 详情页 / iframe 试玩）
- [x] 注册脚本：扫描 `games/` 自动生成注册表
- [x] 第一批 10 款游戏覆盖 8 个分类
- [x] 静态检查接入正式构建与现有自动部署流程
- [x] 扩充到 50 款并更新注册表
- [ ] 逐款完成深度玩法、难度与长时间游玩验收
- [x] 「AI 创作」创作页 UI（选分类 → 一句话描述 → 生成 → 沙箱预览；后端未接入前自动进入演示模式）
- [x] AI 服务配置页（`/#/settings`，⚙️ 入口）：供应商 / Base URL / key / 模型随时可换
- [ ] 生成后端：`/api/generate` 云函数（模板 prompt 组装 → LLM 生成 → 校验 → 沙箱检测）
- [ ] 用户系统（登录 / 发布 / 评论）

## AI 生成服务接口契约（后端按此实现）

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/ai/settings` | POST | 保存 LLM 配置（provider/baseUrl/model/apiKey/dailyLimit）；**key 只存服务端，端点必须加管理鉴权** |
| `/api/ai/settings` | GET | 读取配置，key 以掩码返回（`****abcd`） |
| `/api/ai/test` | POST | 用当前配置发最小 chat 请求，返回 `{ok, message}` |
| `/api/generate` | POST | `{category, prompt}` → `{id, entry, name, emoji, summary}`；创作页已按此对接 |

后端未上线时：创作页自动进入演示模式；配置页把 key 本地暂存在浏览器 `localStorage`（`hub-ai-settings`），并在页面明确标注。
