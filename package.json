{
  "name": "game-hub",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "registry": "node scripts/build-registry.js",
    "check": "node scripts/check-games.js",
    "predev": "npm run registry",
    "dev": "vite",
    "prebuild": "npm run check && npm run registry",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.0"
  }
}
